{{- if .Values.jobScheduler.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "businessplanner.fullname" . }}-job-scheduler
  labels:
    {{- include "businessplanner.labels" . | nindent 4 }}
    app.kubernetes.io/component: job-scheduler
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.jobScheduler.port }}
      targetPort: http
  selector:
    {{- include "businessplanner.selectorLabels" (dict "ctx" . "component" "job-scheduler") | nindent 4 }}
{{- end -}}
