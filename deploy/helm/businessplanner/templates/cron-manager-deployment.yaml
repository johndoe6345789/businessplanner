{{- if .Values.cronManager.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "businessplanner.fullname" . }}-cron-manager
  labels:
    {{- include "businessplanner.labels" . | nindent 4 }}
    app.kubernetes.io/component: cron-manager
spec:
  replicas: {{ .Values.cronManager.replicas }}
  selector:
    matchLabels:
      {{- include "businessplanner.selectorLabels" (dict "ctx" . "component" "cron-manager") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "businessplanner.selectorLabels" (dict "ctx" . "component" "cron-manager") | nindent 8 }}
    spec:
      containers:
        - name: cron-manager
          image: {{ include "businessplanner.image" (dict "ctx" . "image" .Values.cronManager.image) }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.cronManager.port }}
          env:
            - name: DB_HOST
              value: {{ include "businessplanner.fullname" . }}-postgres
            - name: KAFKA_BROKERS
              value: {{ include "businessplanner.fullname" . }}-kafka:{{ .Values.kafka.port }}
{{- end -}}
