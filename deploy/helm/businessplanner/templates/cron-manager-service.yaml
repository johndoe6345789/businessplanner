{{- if .Values.cronManager.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "businessplanner.fullname" . }}-cron-manager
  labels:
    {{- include "businessplanner.labels" . | nindent 4 }}
    app.kubernetes.io/component: cron-manager
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.cronManager.port }}
      targetPort: http
  selector:
    {{- include "businessplanner.selectorLabels" (dict "ctx" . "component" "cron-manager") | nindent 4 }}
{{- end -}}
