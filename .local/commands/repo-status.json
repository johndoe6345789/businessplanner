{
  "name": "repo-status",
  "description": "Show package repo stack status",
  "steps": [
    {
      "type": "check",
      "command": "docker info",
      "error": "Docker daemon is not running"
    },
    {
      "type": "capture",
      "command": "docker inspect -f '{{.State.Status}}' businessplanner-packagerepo 2>/dev/null || echo 'not found'",
      "var": "repo_state"
    },
    {
      "type": "capture",
      "command": "docker inspect -f '{{.State.Status}}' businessplanner-packagerepo-db 2>/dev/null || echo 'not found'",
      "var": "db_state"
    },
    {
      "type": "print",
      "text": "[repo] businessplanner-packagerepo: {repo_state}"
    },
    {
      "type": "print",
      "text": "[repo] businessplanner-packagerepo-db: {db_state}"
    },
    {
      "type": "print",
      "text": "[repo] API URL: http://localhost:5050"
    }
  ]
}
