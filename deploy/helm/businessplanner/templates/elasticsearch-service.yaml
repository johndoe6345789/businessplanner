{{- if .Values.elasticsearch.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "businessplanner.fullname" . }}-elasticsearch
  labels:
    {{- include "businessplanner.labels" . | nindent 4 }}
    app.kubernetes.io/component: elasticsearch
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.elasticsearch.port }}
      targetPort: http
  selector:
    {{- include "businessplanner.selectorLabels" (dict "ctx" . "component" "elasticsearch") | nindent 4 }}
{{- end -}}
